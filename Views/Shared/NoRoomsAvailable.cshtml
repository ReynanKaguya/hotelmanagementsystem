@{
    ViewData["Title"] = "No Available Rooms";
}

<div class="container text-center mt-5">
    <h2 class="text-danger">❌ No Available Rooms</h2>
    <p>We're sorry, but there are no vacant rooms at the moment.</p>
    
    <a href="@Url.Action("Index", "Hotels")" class="btn btn-primary">🏠 Return to Home</a>
</div>
