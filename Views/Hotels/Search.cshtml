@{
    ViewData["Title"] = "Search Available Rooms";
}

<div class="container mt-4">
    <h2 class="text-center fw-bold text-primary">🔍 Find Your Perfect Stay</h2>
    <p class="text-center text-muted">Choose your preferred room and stay dates to check availability.</p>

    <div class="row justify-content-center">
        <div class="col-md-6">
            <form method="post" action="@Url.Action("SearchResults", "Hotels")" class="p-4 border rounded bg-light shadow">
                
                <!-- 🏨 Room Type -->
                <div class="mb-3">
                    <label class="form-label fw-bold">🏨 Room Type:</label>
                    <select name="roomType" class="form-select">
                        <option value="Deluxe">Deluxe</option>
                        <option value="Standard">Standard</option>
                        <option value="Suite">Suite</option>
                    </select>
                </div>

                <!-- 📍 Location -->
                <div class="mb-3">
                    <label class="form-label fw-bold">📍 Location:</label>
                    <input type="text" name="location" class="form-control" placeholder="Enter a city or hotel name" required>
                </div>

                <!-- 📅 Check-in Date -->
                <div class="mb-3">
                    <label class="form-label fw-bold">📅 Check-in Date:</label>
                    <input type="date" name="checkin" class="form-control" required>
                </div>

                <!-- 📅 Check-out Date -->
                <div class="mb-3">
                    <label class="form-label fw-bold">📅 Check-out Date:</label>
                    <input type="date" name="checkout" class="form-control" required>
                </div>

                <!-- 🔍 Search Button -->
                <button type="submit" class="btn btn-primary w-100">
                    <i class="bi bi-search"></i> Search
                </button>
            </form>
        </div>
    </div>
</div>
