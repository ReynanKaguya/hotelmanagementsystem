@model Hotel

<div class="container mt-5">
    <div class="row">
        <div class="col-md-6">
            <img src="@Url.Content("~/images/" + Model.ImageUrl)" class="img-fluid rounded shadow" 
                alt="@Model.Name" style="max-height: 400px; object-fit: cover;">
        </div>
        <div class="col-md-6">
            <h2>@Model.Name</h2>
            <p class="text-muted">@Model.Location</p>
            <p>@Model.Description</p>
            <h4 class="text-success">₱@Model.PricePerNight.ToString("N2") / night</h4>
            <a href="#" class="btn btn-primary">Book Now</a>
        </div>
    </div>
</div>
